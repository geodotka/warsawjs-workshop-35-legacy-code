const Timer = require('../../src/pomodoro/timer');

console.log('Timer', Timer);

describe('Timer', () => {
    describe('minusFunct', () => {
        it('......', () => {
            console.log('---');
        });
    });
});
